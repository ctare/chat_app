<%= form_for @user, html: {class: 'form-group'} do |f| %>
    <%= f.label :name, '名前' %>
    <%= f.text_field :name, class: 'form-control' %>
    <%= f.submit class: %w(btn btn-primary center-position) %>
<% end %>
